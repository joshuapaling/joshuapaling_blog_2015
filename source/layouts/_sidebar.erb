<!-- Side Widget Well -->
<div class="well">
  <h4>Hi! I'm Joss.</h4>
  <p>This blog is just a random bunch of disorganised stuff, which I keep for future reference. I don't even know why I put it online.</p>
  <p>This is what I look like:</p>
  <%= image_tag "profile.png", style: 'max-width: 350px; width: 100%;' %>
</div>

<!-- Blog Categories Well -->
<div class="well">
  <h4>Categories</h4>
  <div class="row">
    <div class="col-lg-12">
      <ul class="list-unstyled">
        <% blog.tags.each do |tag, articles| %>
          <li><%= link_to "#{tag} (#{articles.size})", tag_path(tag) %></li>
        <% end %>
      </ul>
    </div>
  </div>
  <!-- /.row -->
</div>
